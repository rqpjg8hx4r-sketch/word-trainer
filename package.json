{
  "name": "word-trainer",
  "version": "2.16.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-homework-index.js",
    "test": "node --test tests/content.test.js && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
